<li>
    <p>
        <% if (locals.user && locals.user.id == comment.user.id ){ %>
            <small>
                <a href="/comments/destory/<%= comment.id %>">X</a>
            </small>
        <% } %>
        <%= comment.content %>
        <br>
        <%= comment.user %>
    </p>
</li>